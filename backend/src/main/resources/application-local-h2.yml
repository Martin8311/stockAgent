spring:
  datasource:
    url: ${DB_URL:jdbc:h2:file:./.dev/h2/harness_agent;MODE=MySQL;DATABASE_TO_LOWER=TRUE;CASE_INSENSITIVE_IDENTIFIERS=TRUE;DB_CLOSE_DELAY=-1}
    username: ${DB_USERNAME:sa}
    password: "${DB_PASSWORD:}"
    driver-class-name: org.h2.Driver
